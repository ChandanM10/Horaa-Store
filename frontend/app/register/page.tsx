'use client';
import {FormEvent,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {useAuth} from '@/components/AuthProvider';
export default function Register(){const {register}=useAuth();const router=useRouter();const [f,setF]=useState({name:'',email:'',phone:'',password:''});const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setError('');try{await register(f.name,f.email,f.phone,f.password);router.push('/account')}catch(err){setError(err instanceof Error?err.message:'Registration failed')}};return <main className="auth-page container"><form className="auth-card" onSubmit={submit}><div className="brand"><span className="brand-mark">H</span><span>HORAA STORE</span></div><h1>Create your account.</h1><p>Save builds, track orders and keep a hardware wishlist.</p>{(['name','email','phone','password'] as const).map(k=><label key={k}>{k[0].toUpperCase()+k.slice(1)}<input type={k==='password'?'password':k==='email'?'email':'text'} required value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}{error&&<div className="error">{error}</div>}<button className="primary full-btn">Create account</button><small>Already registered? <Link href="/login">Sign in</Link></small></form></main>}
